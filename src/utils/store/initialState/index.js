import { initialBoardState as board } from "./board";
import { initialLevelParams as level } from "./params";
import { initialRoundState as round, initialTimerState as timer } from "./round";
import { initialUserStats as userStats } from "./userStats";

export const initialState = { board, level, round, timer, userStats };
